# AI Gen Image Project

## Project Overview

This is a Python project for AI-generated image processing and generation.

## 技术栈

- **Runtime**: Python (via conda environment at `D:\miniconda\envs\spider\python.exe`)
- **Package Manager**: pip/conda

## 开发规范

### Git提交规范

使用 conventional commits 格式：

```
type(scope): short summary

Types: feat, fix, docs, style, refactor, test, chore
```

第一条消息限制50字符以内，使用祈使语气。

## 项目结构

待补充 - 项目初始化中

## 注意事项

- 所有 Python 程序默认使用 conda 环境：`D:\miniconda\envs\spider\python.exe`
- 运行命令时使用完整路径或确保环境变量正确配置